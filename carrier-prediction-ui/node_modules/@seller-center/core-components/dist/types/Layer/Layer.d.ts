import React from "react";
/**
 * Portals provide a way to render children into a DOM node
 * that exists outside the DOM hierarchy of the parent component.
 */
export declare const Layer: React.ForwardRefExoticComponent<LayerProps & React.RefAttributes<any>>;
export interface LayerProps {
    /**
     * Content to be rendered
     */
    children: any;
    /**
     * Node where React portal will be mounted
     */
    mountNode: Element;
    /**
     * Callback function when Layer is mounted
     */
    onMount?: Function;
    /**
     * Callback function when Layer is Unmounted
     */
    onUnmount?: Function;
    /**
     * If don't want to use React portal
     * @default false
     */
    disablePortal?: boolean;
}
