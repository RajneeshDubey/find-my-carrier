import React from "react";
import { StatefullPopoverProps, Trigger } from "./StatefullPopover";
import { PopoverPosition } from "./PopoverPortal";
export interface TooltipProps extends Omit<StatefullPopoverProps, "trigger" | "hasNubbin" | "autoFocus" | "position"> {
    /**
     * @default hover
     */
    trigger?: Trigger;
    /**
     * @default true
     */
    hasNubbin?: boolean;
    /**
     * @default false
     */
    autoFocus?: boolean;
    /**
     * @default auto
     */
    position?: PopoverPosition;
}
export declare const Tooltip: React.FunctionComponent<TooltipProps>;
