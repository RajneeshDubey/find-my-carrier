import * as React from "react";
import { PopoverPortalProps } from "./PopoverPortal";
export interface PopoverProps extends Omit<PopoverPortalProps, "popoverContainerRef" | "triggerRef"> {
    /**
     * The trigger for the popover.
     */
    children: React.ReactNode;
    /**
     * If the popover is open.
     *
     * @default false
     */
    isOpen?: boolean;
    /**
     * Wrapper class name for container span
     *
     * @default undefined
     */
    containerClassName?: string;
}
export declare const Popover: React.FunctionComponent<PopoverProps>;
