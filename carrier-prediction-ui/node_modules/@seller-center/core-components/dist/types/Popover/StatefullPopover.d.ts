import * as React from "react";
import { PopoverPortalProps } from "./PopoverPortal";
export declare type Trigger = "hover" | "click";
export interface StatefullPopoverProps extends Omit<PopoverPortalProps, "popoverContainerRef" | "triggerRef" | "onClose"> {
    /**
     * The trigger for the popover.
     */
    children: React.ReactNode;
    /**
     * Type of trigger used to open
     * @default "click"
     * */
    trigger?: Trigger;
    /**
     * Delay in opening popover for when trigger is Hover
     * @default 0
     */
    showDelay?: number;
    /**
     * Delay in closing popover for when trigger is Hover
     * @default 200
     */
    hideDelay?: number;
    /**
     * Initial State of Popover
     * @default false
     * */
    inititalState?: boolean;
    /**
     * Callback when popover is opened
     */
    onOpen?: Function;
    /**
     * Controlled state of popover
     * @default undefined
     */
    isOpen?: boolean;
    /**
     * Callback when popover is closed
     */
    onClose?: Function;
    /**
     *  Classname to be applied on trigger element container
     */
    containerClassName?: any;
}
export declare const StatefullPopover: React.FunctionComponent<StatefullPopoverProps>;
