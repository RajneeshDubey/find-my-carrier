import React, { FC } from "react";
export declare type BadgeVariant = "primary" | "secondary";
export interface BadgeProps extends React.DetailedHTMLProps<React.HTMLAttributes<HTMLSpanElement>, HTMLSpanElement> {
    /**
     * The content for the tag.
     */
    children: React.ReactNode;
    /**
     * If provided, additional class name to provide on root element.
     */
    className?: string;
    /**
     * The badge's variant.
     *
     * @default 'primary'
     */
    variant?: BadgeVariant;
}
export declare const Badge: FC<BadgeProps>;
