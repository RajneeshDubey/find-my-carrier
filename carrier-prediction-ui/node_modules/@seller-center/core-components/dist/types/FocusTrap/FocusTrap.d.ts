import * as React from "react";
import { ReactFocusLockProps } from "react-focus-lock/interfaces";
export interface FocusTrapProps extends ReactFocusLockProps {
    /**
     * The content for the focus lock.
     */
    children: React.ReactNode;
    /**
     * If provided, additional class name to provide on the root element.
     */
    className?: string;
    /**
     * If focus should be returned to the triggered element.
     *
     * @default true
     */
    hasFocusReturn?: boolean;
}
export declare const FocusTrap: React.FunctionComponent<FocusTrapProps>;
