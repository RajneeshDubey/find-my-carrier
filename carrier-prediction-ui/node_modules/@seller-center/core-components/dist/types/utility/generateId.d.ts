/**
 * Create a no-conflict id.
 *
 * @param {string} scope — The scope used to generate a unique id.
 * @returns {string} — id
 */
declare const generateId: (scope: string) => string;
export default generateId;
