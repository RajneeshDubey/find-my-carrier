import { createElement } from 'react';
import classNames from 'classnames';
import { Search as WrappedIcon } from '../../../../icons/dist/es6/components/Search.js';
import style from './Search.module.scss.js';

var Search = props => {
  var {
    searchValue = "",
    onSearchFilterChange = () => {},
    searchPlaceholder = "|",
    className = "",
    searchInputClassName = ""
  } = props;
  return /*#__PURE__*/createElement("div", {
    className: classNames(style.container, className)
  }, /*#__PURE__*/createElement("input", {
    className: classNames(style.input, searchInputClassName),
    value: searchValue,
    placeholder: searchPlaceholder,
    onChange: e => onSearchFilterChange(e.target.value)
  }), /*#__PURE__*/createElement(WrappedIcon, {
    size: "medium",
    className: classNames(style.search)
  }));
};
Search.displayName = "Search";

export { Search };
