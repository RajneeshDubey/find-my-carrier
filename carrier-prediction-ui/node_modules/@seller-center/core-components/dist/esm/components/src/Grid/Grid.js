import { objectWithoutProperties as _objectWithoutProperties, extends as _extends } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import { createElement, Children, cloneElement } from 'react';
import classNames from 'classnames';
import { applyCommonProps } from '../utility/commonProps.js';
import style from './Grid.module.scss.js';

var Grid = props => {
  var _applyCommonProps = applyCommonProps(props, Grid, true),
      {
    children,
    className,
    hasGutter = false
  } = _applyCommonProps,
      rest = _objectWithoutProperties(_applyCommonProps, ["children", "className", "hasGutter"]);

  return /*#__PURE__*/createElement("div", _extends({
    className: classNames(style.grid, hasGutter && style.gutter, className)
  }, rest), Children.map(children, child => {
    return /*#__PURE__*/cloneElement(child, {
      hasGutter
    });
  }));
};

export { Grid };
