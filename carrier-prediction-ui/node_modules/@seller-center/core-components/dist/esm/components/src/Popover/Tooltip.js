import { objectWithoutProperties as _objectWithoutProperties, extends as _extends } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import React from 'react';
import { StatefullPopover } from './StatefullPopover.js';
import style from './Tooltip.module.scss.js';

var Tooltip = props => {
  var {
    overrides = {},
    trigger = "hover",
    hasNubbin = true,
    position = "auto",
    autoFocus = false
  } = props,
      rest = _objectWithoutProperties(props, ["overrides", "trigger", "hasNubbin", "position", "autoFocus"]);

  var tooltipOverrides = {
    arrow: [style.arrow, overrides.arrow],
    base: [style.base, overrides.base],
    content: [style.content, overrides.content]
  };
  return /*#__PURE__*/React.createElement(StatefullPopover, _extends({
    overrides: tooltipOverrides,
    trigger: trigger,
    hasNubbin: hasNubbin,
    position: position,
    autoFocus: autoFocus
  }, rest));
};

export { Tooltip };
