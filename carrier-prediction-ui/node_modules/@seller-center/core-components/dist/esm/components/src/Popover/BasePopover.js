import { objectWithoutProperties as _objectWithoutProperties, extends as _extends } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import { createElement } from 'react';
import classNames from 'classnames';
import style from './BasePopover.module.scss.js';

var BasePopover = props => {
  var {
    children,
    className,
    nubbin,
    contentClassName
  } = props,
      rest = _objectWithoutProperties(props, ["children", "className", "nubbin", "contentClassName"]);

  return /*#__PURE__*/createElement("div", _extends({
    className: classNames(style.popover, // TODO: Testcass - Anoop

    /* istanbul ignore next */
    nubbin && style.nubbin,
    /* istanbul ignore next */
    nubbin === "top" && style.top,
    /* istanbul ignore next */
    nubbin === "right" && style.right,
    /* istanbul ignore next */
    nubbin === "left" && style.left,
    /* istanbul ignore next */
    nubbin === "bottom" && style.bottom, className)
  }, rest), /*#__PURE__*/createElement("div", {
    className: classNames(style.content, contentClassName)
  }, children));
};

export { BasePopover };
