import { objectWithoutProperties as _objectWithoutProperties, extends as _extends } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import { createElement } from 'react';
import classNames from 'classnames';
import style from './Tag.module.scss.js';

var Tag = props => {
  var {
    className,
    color = "blue",
    variant = "outlined"
  } = props,
      rest = _objectWithoutProperties(props, ["className", "color", "variant"]);

  return /*#__PURE__*/createElement("span", _extends({
    className: classNames(style.tag, color === "blue" && style.blue, color === "gray" && style.gray, variant === "filled" && style.filled, variant === "outlined" && style.outlined, className)
  }, rest));
};

export { Tag };
