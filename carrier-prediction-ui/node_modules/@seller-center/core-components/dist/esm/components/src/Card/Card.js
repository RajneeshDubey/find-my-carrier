import { objectWithoutProperties as _objectWithoutProperties, extends as _extends } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import { createElement } from 'react';
import classNames from 'classnames';
import style from './Card.module.scss.js';

var Card = _ref => {
  var {
    children,
    className,
    size = "medium"
  } = _ref,
      rest = _objectWithoutProperties(_ref, ["children", "className", "size"]);

  // const { children, className, size = "medium", ...rest } = props;
  return /*#__PURE__*/createElement("div", _extends({
    className: classNames(style.card, size === "medium" && style.medium, size === "large" && style.large, size === "small" && style.small, className)
  }, rest), children);
};

export { Card };
