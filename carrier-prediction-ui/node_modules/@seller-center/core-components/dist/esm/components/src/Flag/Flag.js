import { objectWithoutProperties as _objectWithoutProperties, extends as _extends } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import { createElement } from 'react';
import classNames from 'classnames';
import style from './Flag.module.scss.js';

var Flag = props => {
  var {
    children,
    className,
    color = "blue",
    variant = "outlined"
  } = props,
      rest = _objectWithoutProperties(props, ["children", "className", "color", "variant"]);

  return /*#__PURE__*/createElement("span", _extends({
    className: classNames(style.flag, color === "blue" && style.blue, color === "red" && style.red, variant === "outlined" && style.outlined, variant === "filled" && style.filled, className)
  }, rest), children);
};

export { Flag };
